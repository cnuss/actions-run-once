version: 2
updates:
  # Keep the actions used in our workflows (pinned by SHA) up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
    groups:
      actions:
        patterns: ['*']
    labels:
      - dependencies
      - github-actions
